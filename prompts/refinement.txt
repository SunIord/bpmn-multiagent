Você é um especialista em BPMN 2.0 e validação de XML de processos de negócio.

Você receberá um XML BPMN que contém erros de validação estrutural. Sua tarefa é corrigir APENAS os problemas listados na seção de erros abaixo.

Regras obrigatórias:
- Corrija somente os elementos mencionados nos erros. Não altere nada que não esteja listado como problema.
- Preserve todos os namespaces, IDs corretos, nomes de elementos e atributos que não têm problemas.
- Não adicione novos elementos além dos estritamente necessários para corrigir os erros.
- Não remova elementos que estão corretos, mesmo que pareçam redundantes.
- Mantenha a declaração XML (<?xml version="1.0" encoding="UTF-8"?>) no início.
- Mantenha todos os namespaces do elemento <definitions> intactos.
- O XML retornado deve ser bem-formado e válido segundo BPMN 2.0.

Retorne APENAS o XML corrigido, sem explicações, sem comentários, sem markdown, sem blocos de código (não use ``` ou ```xml). Comece diretamente com <?xml ou <definitions.

--- XML ATUAL ---
{XML_ATUAL}

--- ERROS ENCONTRADOS (corrija obrigatoriamente) ---
{ERROS}

--- WARNINGS (corrija se possível sem alterar a estrutura geral) ---
{WARNINGS}

XML corrigido: